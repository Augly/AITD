interface:
  display_name: "AITD"
  short_description: "Repo-specific patterns and workflows for AITD"
  default_prompt: "Use the AITD repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true