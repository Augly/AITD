实现完成。以下是总结：

## 修改内容

### 1. `backend/utils.py` — 新增统一的 `parse_klines()` 函数
- 支持参数化配置：`reverse`、`quote_volume_index`、`close_time_index`、`min_length`
- 兼容 3 种交易所变体：Binance、Bybit、OKX
- 保留 `close_value` 为 None 时跳过行的逻辑
- 保留 `int(num(row[0]) or 0)` 类型转换行为
- OKX 的 `quoteVolume` 回退逻辑（`row[7]` 或 `row[6]`）

### 2. 替换 4 处重复实现
| 文件 | 变更 |
|------|------|
| `backend/exchanges/binance.py` | `_parse_klines()` → 调用 `parse_klines(rows)` |
| `backend/exchanges/bybit.py` | inline 解析 → `parse_klines(..., reverse=True, quote_volume_index=6, close_time_index=None, min_length=7)` |
| `backend/exchanges/okx.py` | inline 解析 → `parse_klines(..., reverse=True, quote_volume_index=7, close_time_index=None, min_length=8)` |
| `backend/market.py` | 本地实现 → 转发到 `utils.parse_klines` |

### 3. 新增测试 `tests/test_parse_klines.py`
- 21 个测试用例，覆盖 Binance / Bybit / OKX 三种模式及边界情况
- 全部 106 个测试通过，无回归

## Learnings

- **模式**: 项目交易所网关遵循统一的 `ExchangeGateway` 基类，参数化配置是消除重复的有效方式
- **经验**: `market.py` 中保留同名 `parse_klines` 作为向后兼容的薄包装，避免破坏外部调用者
