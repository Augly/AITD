子任务 T-001 已完成。

## 变更摘要

- **`backend/exchanges/base.py`**：新增 `_cache_policy_for_kline_interval` 具体方法（+14 行）
- **`backend/exchanges/binance.py`**：删除重复方法（-14 行）
- **`backend/exchanges/bybit.py`**：删除重复方法（-14 行）
- **`backend/exchanges/okx.py`**：删除重复方法（-14 行）

## 验证结果

- 全部 106 个测试通过
- 净代码减少 42 行（消除 3 处完全相同的重复实现）
- 三个网关类自动继承基类方法，行为不变

## Learnings

- **模式**: 三个网关的 `_cache_policy_for_kline_interval` 实现逐行相同，是典型的"复制粘贴"重复。提取到基类后，后续修改缓存策略只需改一处。
- **经验**: 基类 `ExchangeGateway` 中已有具体方法（如 `normalize_symbol`、`candidate_symbol_hint`），新增 `_cache_policy_for_kline_interval` 符合现有设计模式。子类中同名方法会覆盖基类实现，删除后自动继承。
- **注意**: Issue 标题还提到 `_query` 和 `base_asset_from_symbol` 也有重复，但根据当前子任务 T-001 的要求，本次只处理了 `_cache_policy_for_kline_interval`。`_query` 在 binance/bybit/okx 中的实现略有不同（okx 有额外的 `_query_string` 辅助方法），`base_asset_from_symbol` 的 okx 实现与其他两个不同（使用 `-` 分隔符），这些需要后续子任务评估是否适合提取。
