基于代码库分析，Issue #17 的核心是将 `backend/engine_core.py` 中 253 行的顺序执行交易循环解耦为事件驱动架构。以下是子任务拆分：

---

## Issue #17 子任务拆分

### 背景
当前 `run_trading_cycle()` 是一个 253 行的顺序调用函数，16 个步骤紧密耦合。引入 EventBus 后，每个步骤变为独立的事件处理器，支持监听、日志、告警。

---

### T-001: 定义 EventBus 核心接口与事件类型
**描述**: 创建 `backend/engine/events.py`，实现线程安全的 EventBus 类。支持事件订阅（`on`/`off`）、发布（`emit`）、同步分发。定义交易循环相关的事件类型常量（如 `cycle.started`, `state.loaded`, `decision.made`, `position.opened`, `cycle.completed` 等）。

**验收条件**:
- EventBus 使用标准库实现，无外部依赖
- 支持多订阅者，使用 `threading.Lock` 保证线程安全
- 事件类型覆盖交易循环的 16 个主要阶段
- 包含基础单元测试验证订阅/发布/取消订阅功能

---

### T-002: 重构交易循环为事件驱动流程
**描述**: 修改 `backend/engine_core.py` 中的 `run_trading_cycle()`，将顺序调用改为事件发布模式。每个阶段完成后发布对应事件，通过 EventBus 串联流程。保持现有业务逻辑不变，仅改变调用方式。

**验收条件**:
- 交易循环各阶段通过事件触发，而非直接函数调用
- 原有业务逻辑行为完全一致（所有现有测试通过）
- `run_trading_cycle()` 行数减少至少 30%（从 253 行降至 180 行以下）
- 新增事件流的可观测性（可通过日志追踪事件流转）

---

### T-003: 实现事件监听器（日志/告警/指标）
**描述**: 创建 `backend/engine/event_listeners.py`，实现三类监听器：1) 结构化日志监听器（记录每个事件到日志文件）；2) 告警监听器（对 `circuit_breaker.triggered`, `protection.hit` 等关键事件发送告警）；3) 指标统计监听器（统计各阶段耗时、事件频次）。

**验收条件**:
- 日志监听器输出结构化 JSON 日志（包含事件名、时间戳、上下文）
- 告警监听器对关键事件执行告警动作（至少实现打印/日志告警，预留 webhook 接口）
- 指标监听器能统计并输出各阶段耗时
- 监听器通过 EventBus 订阅，与业务逻辑完全解耦
- 包含单元测试验证监听器行为

---

### T-004: 集成 EventBus 到 AppRuntime 并验证端到端
**描述**: 在 `backend/server.py` 的 `AppRuntime` 中初始化 EventBus，注册监听器，确保交易循环启动时事件系统正常工作。编写集成测试验证完整事件流。

**验收条件**:
- `AppRuntime` 启动时自动初始化 EventBus 并注册所有监听器
- 一次完整的交易循环能触发所有预期事件
- 集成测试验证事件顺序正确、无遗漏
- 运行完整测试套件（`pytest`）全部通过
- 更新相关文档（如有）

---

```json
{
  "issueNumber": 17,
  "subtasks": [
    {
      "id": "T-001",
      "title": "定义 EventBus 核心接口与事件类型",
      "description": "创建 backend/engine/events.py，实现线程安全的 EventBus 类。支持事件订阅（on/off）、发布（emit）、同步分发。定义交易循环相关的事件类型常量（如 cycle.started, state.loaded, decision.made, position.opened, cycle.completed 等）。使用标准库实现，无外部依赖。",
      "acceptanceCriteria": [
        "EventBus 使用标准库实现，无外部依赖",
        "支持多订阅者，使用 threading.Lock 保证线程安全",
        "事件类型覆盖交易循环的 16 个主要阶段",
        "包含基础单元测试验证订阅/发布/取消订阅功能"
      ],
      "priority": 1,
      "type": "code",
      "passes": false
    },
    {
      "id": "T-002",
      "title": "重构交易循环为事件驱动流程",
      "description": "修改 backend/engine_core.py 中的 run_trading_cycle()，将顺序调用改为事件发布模式。每个阶段完成后发布对应事件，通过 EventBus 串联流程。保持现有业务逻辑不变，仅改变调用方式。",
      "acceptanceCriteria": [
        "交易循环各阶段通过事件触发，而非直接函数调用",
        "原有业务逻辑行为完全一致（所有现有测试通过）",
        "run_trading_cycle() 行数减少至少 30%",
        "新增事件流的可观测性（可通过日志追踪事件流转）"
      ],
      "priority": 2,
      "type": "code",
      "passes": false
    },
    {
      "id": "T-003",
      "title": "实现事件监听器（日志/告警/指标）",
      "description": "创建 backend/engine/event_listeners.py，实现三类监听器：1) 结构化日志监听器（记录每个事件到日志文件）；2) 告警监听器（对 circuit_breaker.triggered, protection.hit 等关键事件发送告警）；3) 指标统计监听器（统计各阶段耗时、事件频次）。",
      "acceptanceCriteria": [
        "日志监听器输出结构化 JSON 日志（包含事件名、时间戳、上下文）",
        "告警监听器对关键事件执行告警动作（至少实现打印/日志告警，预留 webhook 接口）",
        "指标监听器能统计并输出各阶段耗时",
        "监听器通过 EventBus 订阅，与业务逻辑完全解耦",
        "包含单元测试验证监听器行为"
      ],
      "priority": 3,
      "type": "code",
      "passes": false
    },
    {
      "id": "T-004",
      "title": "集成 EventBus 到 AppRuntime 并验证端到端",
      "description": "在 backend/server.py 的 AppRuntime 中初始化 EventBus，注册监听器，确保交易循环启动时事件系统正常工作。编写集成测试验证完整事件流。",
      "acceptanceCriteria": [
        "AppRuntime 启动时自动初始化 EventBus 并注册所有监听器",
        "一次完整的交易循环能触发所有预期事件",
        "集成测试验证事件顺序正确、无遗漏",
        "运行完整测试套件（pytest）全部通过",
        "更新相关文档（如有）"
      ],
      "priority": 4,
      "type": "code",
      "passes": false
    }
  ]
}
```
