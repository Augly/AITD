实现完成。以下是总结：

## 完成内容

### 新增文件

1. **`backend/engine/events.py`** — EventBus 核心实现
   - `EventBus` 类：线程安全（`threading.Lock`），标准库实现，零外部依赖
   - 19 个事件类型常量，覆盖交易循环 16 个主要阶段
   - 支持 `on`/`off`/`emit`/`clear`/`listener_count`/`has_listener`
   - 错误隔离：单个 handler 异常不影响其他 handler

2. **`tests/test_eventbus.py`** — 22 个单元测试
   - 事件类型常量验证（2 个测试）
   - 订阅/发布基础功能（4 个测试）
   - 取消订阅（4 个测试）
   - 线程安全并发测试（3 个测试）
   - 错误处理（2 个测试）
   - 内省 API（5 个测试）
   - 清空功能（2 个测试）

### 修改文件

- **`backend/engine/__init__.py`** — 导出 EventBus 和全部事件类型常量

### 验证结果

- 新增 22 个测试全部通过
- 原有 155 个测试全部通过，无回归
- 总计 177 个测试通过

## Learnings

- **模式**: 项目使用 `from __future__ import annotations` 和类型注解，测试采用 pytest 风格（类组织 + 实例方法）
- **经验**: `backend/engine/` 是 engine_core.py 的拆分目标目录，新增模块应在此目录下，并通过 `__init__.py` 导出
- **经验**: EventBus 采用同步分发设计，与项目现有的同步交易循环模型一致；后续子任务（如异步分发、持久化）可在此基础上扩展
