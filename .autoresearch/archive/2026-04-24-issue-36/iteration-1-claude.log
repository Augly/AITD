所有测试通过。让我做最终总结。

---

## 实现完成

### 修改内容

1. **新建 `backend/sandbox.py`** - 安全沙箱模块
   - `SAFE_BUILTINS` - 移除 `__import__`、`eval`、`exec`、`compile`、`open` 等危险函数的安全 builtins 字典
   - `run_restricted_python()` - 受限执行函数，支持白名单模块导入、超时机制
   - `SecurityError` - 安全违规异常
   - 使用线程实现超时控制，防止无限循环

2. **新建 `backend/test_sandbox.py`** - 26 个单元测试
   - 验证合法代码正常执行
   - 验证危险 builtins 被阻断
   - 验证非白名单模块导入被阻断
   - 验证超时机制
   - 验证异常传播

3. **修改 `backend/market.py:140`**
   - 将 `exec(source_code, scope)` 替换为 `run_restricted_python(source_code, timeout_seconds=30.0)`
   - 移除不安全的 `__builtins__` 注入

### 自检结果

- [x] 代码语法检查通过
- [x] 26 个单元测试全部通过
- [x] 集成测试通过（模拟动态候选源场景）
- [x] 安全漏洞已修复（`__import__`、`eval`、`exec`、`compile`、`open` 均被阻断）

## Learnings

- **模式**: Python `exec()` 沙箱化需要同时处理 `__builtins__` 和 `__import__` 两个入口，仅限制 `__builtins__` 不够
- **踩坑**: Python 类定义依赖内部函数 `__build_class__`，构建安全 builtins 时必须保留它
- **经验**: 使用线程 + `threading.Event.wait(timeout)` 是实现超时的跨平台方案（`signal` 仅在主线程有效）
