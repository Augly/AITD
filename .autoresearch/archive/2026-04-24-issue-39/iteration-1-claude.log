实现完成。让我总结本次工作：

## 实现总结

### 修改的文件

1. **`backend/server.py:713-747`** - 修复 `_serve_static` 路径遍历漏洞
   - 新增 symlink 检测：遍历 `file_path.parents` 检查每个目录和文件本身是否为 symlink
   - 新增 `os.path.commonpath` 二次验证：确保解析后的路径严格在 `DASHBOARD_DIR` 下
   - 保留原有的 `parents` 检查作为纵深防御

2. **`backend/engine/state.py`** - 修复因 `utils.py` 中 `read_json_locked`/`write_json_locked` 被移除导致的导入错误
   - 将 `read_json_locked` → `read_json`
   - 将 `write_json_locked` → `write_json`

3. **`tests/test_server_static.py`** - 新增 7 个测试用例
   - `test_normal_file_served` - 正常文件请求返回 200
   - `test_path_traversal_blocked` - 路径遍历返回 403
   - `test_symlink_in_path_blocked` - symlink 文件被阻止
   - `test_symlink_parent_directory_blocked` - symlink 目录被阻止
   - `test_commonpath_validation_blocks_escape` - commonpath 验证阻止逃逸
   - `test_nonexistent_file_returns_404` - 不存在文件返回 404
   - `test_root_path_serves_index` - 根路径返回 index.html

### 自检结果

- [x] 代码可以编译/类型检查通过
- [x] 相关测试通过（7/7 新测试 + 21/21 现有 server 测试 + 44/44 其他后端测试）
- [x] 无安全漏洞

## Learnings

- **踩坑**: 项目存在 `backend/engine/state.py` 引用已删除的 `read_json_locked`/`write_json_locked` 函数的遗留问题，导致任何导入 `backend.server` 的测试都无法运行。修复方式是回退到 `read_json`/`write_json`。
- **经验**: 在修改代码前应先验证测试环境是否可运行，避免在已损坏的代码基上工作。
- **模式**: 路径遍历防御应采用多层验证：symlink 检测 + `commonpath`  containment + `parents` 检查，形成纵深防御。
