实现完成。让我做最终验证并总结。

### 提交前自检 Checklist

- [x] 代码可以编译/类型检查通过 — Python 语法编译通过
- [x] 相关测试通过 — 23 个测试全部通过（8 个新测试 + 15 个已有测试）
- [x] 新代码有对应的测试覆盖 — 8 个新测试覆盖缓存核心逻辑
- [x] 无安全漏洞 — 无用户输入处理，仅内部缓存机制
- [x] 错误处理完整 — `validate_symbol` 保持原有的 try/except 兜底

---

## 实现总结

### 修改内容

**`backend/exchanges/binance.py`**:

1. **`__init__`** (第 43-44 行): 新增实例级缓存字段
   - `_symbol_info_cache: dict[str, dict[str, Any]]` — symbol → info 映射
   - `_symbol_info_cache_expiry: float` — 缓存过期时间戳

2. **`_symbol_info_cache_refresh`** (第 294-304 行): 缓存刷新方法
   - 检查缓存是否过期（TTL = 6小时，与文件缓存对齐）
   - 过期时调用 `_exchange_info` 并构建映射字典
   - 更新过期时间戳

3. **`_clear_symbol_cache`** (第 306-308 行): 显式清理接口
   - 清空缓存字典，重置过期时间

4. **`_symbol_info`** (第 310-316 行): 修改为先查缓存，O(1) 查找
   - 之前: 每次遍历 symbols 列表 O(N)
   - 现在: dict.get() O(1)

5. **`validate_symbol`** (第 72-82 行): 利用内存缓存替代遍历
   - 之前: 单独发请求遍历 symbols 列表
   - 现在: 复用 `_symbol_info_cache_refresh` 的缓存

**`tests/test_binance_symbol_cache.py`**: 新增 8 个测试
- 首次调用触发缓存填充
- 后续调用复用缓存
- TTL 过期自动刷新
- 显式清理接口
- 未知 symbol 抛出异常
- `validate_symbol` 利用缓存
- 正则不匹配返回 False
- TTL 与文件缓存对齐

### 性能改进

- **之前**: 每次 `_symbol_info` 调用 → O(N) 遍历 hundreds of symbols
- **之后**: 首次调用后 → O(1) dict 查找，同实例零额外网络/遍历开销

---

## Learnings

- **模式**: 项目使用 `cached_get_json` 做文件级 HTTP 缓存，但解析后的数据结构遍历仍是性能瓶颈。内存级缓存适合高频、同实例重复访问的场景。
- **模式**: `monkeypatch.setattr` 在 pytest 中可以方便地 mock 实例方法，适合测试缓存命中/未命中逻辑。
- **踩坑**: `validate_symbol` 原有的实现是独立发请求的，改为复用缓存后需要保持 try/except 兜底行为（网络异常时返回 True），避免破坏原有容错语义。
- **经验**: 缓存 TTL 与底层 `cached_get_json` 的 TTL 对齐（6小时），避免内存缓存和文件缓存不同步导致的 stale data 问题。
