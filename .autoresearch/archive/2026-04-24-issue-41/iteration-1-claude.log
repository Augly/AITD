子任务 T-001 实现完成。

## 修改摘要

**文件**: `backend/exchanges/binance.py`

为 `_exchange_info` 添加了实例级内存缓存：

1. `__init__` 新增三个属性：
   - `_exchange_info_cache`: 缓存的 exchange info 数据
   - `_exchange_info_expires_at_ms`: 缓存过期时间戳
   - `_exchange_info_lock`: `threading.Lock()` 保证并发安全

2. `_exchange_info` 方法改为锁内"检查-获取-写入"原子操作：
   - 缓存命中时直接返回内存数据，零 I/O
   - 缓存失效时调用 `cached_get_json`，结果写入内存缓存
   - TTL 6 小时，与现有文件缓存策略一致

**新增测试**: `tests/test_binance_exchange_info_cache.py`（5 个用例）
- 验证缓存命中不重复调用 `cached_get_json`
- 验证 TTL 过期后自动刷新
- 验证 10 线程并发下只穿透一次
- 验证空 payload 返回 `{}`
- 验证 `_symbol_info` 复用缓存

**测试**: 新增 5/5 通过，现有 217/217 通过（8 个已有失败无关）。

---

## Learnings

- **模式**: 两层缓存（内存 + 文件）时，TTL 保持一致避免策略冲突
- **踩坑**: 初始实现将获取逻辑放在锁外，导致并发测试失败（10 线程全部穿透）。修复为锁内完成全部操作后通过。
- **经验**: Python 实例级内存缓存使用 `threading.Lock()` 足够，无需 `RLock`；测试 monkeypatch 应针对模块级函数而非深层依赖
