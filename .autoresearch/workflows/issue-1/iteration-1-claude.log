T-001 子任务已完成。以下是实现总结：

## 修改内容

### `backend/utils.py`
- 新增 `read_json_locked(path, default)` — 使用 `FileLock` 保护读取，异常时返回 default
- 新增 `write_json_locked(path, payload)` — 使用 `FileLock` 保护 + 原子写入（`mkstemp` → `os.replace`）
- 锁文件路径：`{原文件}.lock`
- 敏感文件自动设置 `0o600` 权限

### `requirements.txt`
- 添加 `filelock>=3.0.0` 依赖

### `tests/test_file_lock.py`
- 16 个测试覆盖：正常读写、默认值、错误处理、锁文件创建、敏感文件权限、原子写入、并发写入不损坏、无临时文件残留、锁互斥

## 自检结果

| 检查项 | 状态 |
|--------|------|
| 代码可编译/类型检查 | 通过 |
| 测试通过 | 65/65 全部通过 |
| 无回归 | 通过 |

## Learnings

- **踩坑**: macOS 上 `tempfile.mkstemp` 创建的文件权限固定为 `0o600`，`os.replace` 会保留源权限而非继承目标。非敏感文件需要在 replace 后显式 `chmod` 到 `0o666 & ~umask`
- **踩坑**: macOS Python 3.9 默认使用 `spawn` 多进程模式，不能序列化局部函数/闭包。并发测试需用 `threading` 替代 `multiprocessing`，或将函数定义为模块级
- **经验**: `filelock` 的 `FileLock` 在 macOS 上使用 `fcntl` 实现，跨进程和跨线程都有效，且锁文件无需预先存在
