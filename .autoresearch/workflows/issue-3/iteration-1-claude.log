子任务 T-001 已完成。以下是实现总结：

## 实现总结

### 新建文件

| 文件 | 说明 |
|------|------|
| `backend/routing.py` | RouteRegistry 类 + `@route` 装饰器 |
| `tests/test_routing.py` | 10 个单元测试 |

### API 设计

```python
# backend/routing.py

@route("GET", "/api/latest")
def handle_api_latest(handler: BaseHTTPRequestHandler) -> None:
    ...

# 查找
registry = get_default_registry()
handler = registry.lookup("GET", "/api/latest")  # 返回函数或 None
```

### 质量检查

| 检查项 | 结果 |
|--------|------|
| mypy 类型检查 | 通过 |
| pytest 新测试 | 10/10 通过 |
| 全量回归测试 | 75/75 通过 |
| server.py 修改 | 无（按子任务要求） |

### 设计要点

- **大小写不敏感**: `lookup("get", ...)` 等价于 `lookup("GET", ...)`
- **覆盖语义**: 同一路由重复注册时后者覆盖前者
- **零侵入**: 此阶段不修改 `server.py`，后续子任务再迁移路由

---

## Learnings

- **模式**: 项目使用 `from __future__ import annotations` + 完整 type hints，mypy 是标准质量门
- **踩坑**: 系统默认 `python` 命令不存在，需使用 `python3`
- **经验**: 装饰器模式在此项目中可行 — handler 函数接收 `BaseHTTPRequestHandler` 实例，与现有 `_handle` 中的调用模式一致，后续迁移时只需将 if-elif 体提取为独立函数并加装饰器
