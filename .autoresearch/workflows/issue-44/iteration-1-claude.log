子任务 T-001 已完成。以下是实现总结：

## 完成内容

### 新建文件

1. **`backend/logging_config.py`** — 结构化日志模块
   - `JSONFormatter` — 将日志记录格式化为 JSON，包含 `timestamp`、`level`、`message` 字段
   - `_MemoryLogBuffer` — 线程安全的内存 Handler，缓存最近 400 条日志
   - `configure_logging()` — 配置根 logger，支持通过参数或 `LOG_LEVEL` 环境变量设置级别（默认 INFO）
   - `get_logger()` — 工厂函数，自动配置根 logger
   - `get_memory_entries()` — 获取内存缓冲区中的日志条目

2. **`tests/test_logging_config.py`** — 测试文件（16 个测试用例）
   - 覆盖 JSON 格式化、异常处理、非 ASCII 字符
   - 内存缓冲区容量限制、线程安全、条目结构
   - 配置函数（默认级别、显式级别、环境变量、重复 Handler 防护）
   - 集成测试（级别过滤、JSON 输出格式）

### 质量指标

| 检查项 | 结果 |
|--------|------|
| 测试通过率 | 16/16 ✅ |
| 测试覆盖率 | 100% ✅ |
| 语法检查 | 通过 ✅ |
| 未修改 server.py | ✅ |

## Learnings

- **模式**: 项目中使用 `threading.Lock()` 保护共享状态（如 `AppRuntime.lock`），`_MemoryLogBuffer` 遵循相同模式
- **踩坑**: `logging.LogRecord` 的 `exc_info` 参数需要传入 `sys.exc_info()` 元组，而非 `True` 布尔值
- **经验**: 测试中使用 `patch` 重置模块内部单例状态，避免测试间污染
