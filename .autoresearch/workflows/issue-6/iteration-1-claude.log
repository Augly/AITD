实现完成。以下是总结：

## 修改内容

**`backend/utils.py`**:
- 添加 `os` 模块导入
- 新增 `SENSITIVE_CONFIG_FILES = {"live_trading.json", "llm_provider.json"}` 常量
- 修改 `write_json()` 函数：写入后检测文件名，如果是敏感配置文件则设置 `0o600` 权限

**`tests/test_write_json_permissions.py`** (新增):
- 6 个测试用例覆盖所有验收条件

## 验收条件验证

| 条件 | 状态 |
|------|------|
| `live_trading.json` 写入后权限为 `0o600` | ✅ |
| `llm_provider.json` 写入后权限为 `0o600` | ✅ |
| 普通配置文件（如 `dashboard_settings.json`）权限不受影响 | ✅ |
| 已有密钥文件在下次写入时权限修正为 `0o600` | ✅ |

## Learnings

- **模式**: 项目使用 `write_json()` 作为统一的 JSON 配置文件写入入口，只需在此一处修改即可覆盖所有敏感文件的权限控制。
- **经验**: 通过文件名集合判断敏感文件的方式简洁且易于扩展，后续如需新增敏感配置文件只需添加到 `SENSITIVE_CONFIG_FILES` 集合中。
- **踩坑**: macOS 上 `python` 命令不可用，需使用 `python3`。
