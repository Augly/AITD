所有 49 个测试通过。让我总结本次实现。

## 实现完成：T-001 提取状态管理模块

### 变更摘要

1. **新建 `backend/engine/__init__.py`** — engine 包初始化，导出状态相关函数
2. **新建 `backend/engine/state.py`** — 264 行，包含全部 12 个状态管理函数：
   - `clean_mode`, `empty_trading_account`, `default_state`
   - `normalize_position`, `normalize_trade`, `normalize_exchange_closed_trade`, `normalize_order`
   - `derive_session_started_at`, `normalize_decision`
   - `read_trading_state`, `write_trading_state`, `archive_decision`
3. **重命名 `backend/engine.py` → `backend/engine_core.py`** — 避免与 `backend/engine/` 包命名冲突（Python 包优先于模块）
4. **修改 `backend/engine_core.py`** — 移除状态函数定义，改为从 `.engine.state` 导入；行数从 1670+ 减至 1431
5. **修改 `backend/server.py`** — 导入路径从 `.engine` 更新为 `.engine_core`

### 验证结果

- ✅ 所有 49 个现有测试通过
- ✅ `backend/engine/state.py` 包含所有状态相关函数
- ✅ `engine_core.py` 通过 import 使用状态模块
- ✅ 状态读写行为与拆分前完全一致

### 文件结构

```
backend/
├── engine/
│   ├── __init__.py      # 包导出
│   ├── state.py         # 状态管理模块 (264 行)
│   └── CLAUDE.md        # 目录级知识文档
├── engine_core.py       # 原 engine.py，移除状态函数后 (1431 行)
└── server.py            # 更新导入路径
```

## Learnings

- **模式**: Python 包（`engine/`）和模块（`engine.py`）不能共存——包优先。拆分 God Class 时需要提前规划命名，避免导入冲突。
- **踩坑**: 最初创建 `backend/engine/` 包后，`from .engine import ...` 在 server.py 中解析到了包而非模块，导致导入失败。解决方案是将原 `engine.py` 重命名为 `engine_core.py`。
- **经验**: 对于大型模块拆分，建议先创建新模块、再修改旧模块、最后更新所有导入路径，并在每步后运行测试验证。
